# Wait for ANY interface to be online, not all
# This prevents boot delays when some interfaces have no carrier (e.g., unplugged ethernet)
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any
