# PLACEHOLDER: Create AutonomOS A/B SD card image for BeagleBone
# TODO: Configure correct partition layout for BeagleBone storage
#
# Partition layout (placeholder):
#   1. boot (FAT32) - 64MB - MLO, u-boot, kernel, DTB
#   2. rootfs-a (ext4) - 2GB - primary rootfs (slot A)
#   3. rootfs-b (ext4) - 2GB - secondary rootfs (slot B)
#   4. data (ext4) - remaining space - persistent storage

part /boot --source bootimg-partition --ondisk mmcblk0 --fstype=vfat --label boot --active --align 4096 --size 64
part / --source rootfs --ondisk mmcblk0 --fstype=ext4 --label rootfs-a --align 4096 --fixed-size 2048
part --ondisk mmcblk0 --fstype=ext4 --label rootfs-b --align 4096 --fixed-size 2048
part /data --fixed-size 512M --ondisk mmcblk0 --fstype=ext4 --label data --align 4096
