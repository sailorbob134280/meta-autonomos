# PLACEHOLDER: Create AutonomOS A/B SD card image for BeagleBone
# TODO: Configure correct partition layout for BeagleBone storage
#
# Partition layout (placeholder):
#   1. boot (FAT32) - 64MB - MLO, u-boot, kernel, DTB
#   2. rootfs-a (ext4) - ${AUTONOMOS_ROOTFS_SIZE}MB - primary rootfs (slot A)
#   3. rootfs-b (ext4) - ${AUTONOMOS_ROOTFS_SIZE}MB - secondary rootfs (slot B)
#   4. data (ext4) - ${AUTONOMOS_DATA_SIZE}MB initial, grows if grow-data feature enabled
#
# Configure sizes via AUTONOMOS_ROOTFS_SIZE and AUTONOMOS_DATA_SIZE in local.conf

part /boot --source bootimg-partition --ondisk mmcblk0 --fstype=vfat --label boot --active --align 4096 --size 64
part / --source rootfs --ondisk mmcblk0 --fstype=ext4 --label rootfs-a --align 4096 --fixed-size ${AUTONOMOS_ROOTFS_SIZE}
part --ondisk mmcblk0 --fstype=ext4 --label rootfs-b --align 4096 --fixed-size ${AUTONOMOS_ROOTFS_SIZE}
part /data --fixed-size ${AUTONOMOS_DATA_SIZE}M --ondisk mmcblk0 --fstype=ext4 --label data --align 4096
